<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fastcure">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FastCure">
        <activity android:name=".HomeActivity"> </activity>
        <activity android:name=".PatientLoginActivity"> </activity>
        <activity android:name=".PatientRegActivity"> </activity>
        <activity android:name=".ViewTrackerActivity"> </activity>
        <activity android:name=".HealthTrackerActivity"> </activity>
        <activity android:name=".DoctorRegActivity" />
        <activity android:name=".DoctorLoginActivity" />
        <activity android:name=".AddAppActivity" />
        <activity android:name=".AddPreActivity" />
        <activity android:name=".DoctorHomeActivity" />
        <activity android:name=".ViewAppActivity" />
        <activity android:name=".ViewAppDActivity" />
        <activity android:name=".ViewDocActivity" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>